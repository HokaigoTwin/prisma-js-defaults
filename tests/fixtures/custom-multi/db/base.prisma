generator client { 
    provider = "prisma-client-js" 
}

datasource db {
  provider = "postgresql"
}

model BaseSession {
  id Int @id @default(autoincrement())
  /// @defaultJs(generateSessionToken)
  token String @default("")
}